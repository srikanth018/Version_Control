Created a .gitignore file.
Created environment and temporary files and ignored them in .gitignore.
These files are not tracked by Git.
Also created a temp directory to store temporary files, which is ignored.
Also Verified with "git status" to confrim the files are being tracked